apiVersion: v1
kind: Service
metadata:
  name: nextjs-dashboard
  labels:
    app: nextjs-dashboard
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: nextjs-dashboard
